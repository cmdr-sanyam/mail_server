<% if user_signed_in? %>
  <%= button_to 'Log out', destroy_user_session_path, method: :delete, class: 'dropdown-item', title: 'Log out', data: { turbo_confirm: 'Are you sure?' } %>
<% else %>
  <%= link_to 'Log In', new_user_session_path, class: 'link' %>
<% end %>


<h1>Pages#index</h1>
<p>Find me in app/views/pages/index.html.erb</p>
